import { PlaceholderPage } from "../../../shared/components/PlaceholderPage";

export function SignalsPage() {
   return (
      <PlaceholderPage
         title="Signals"
         description="Historial de señales generadas, ejecutadas, ignoradas o rechazadas."
      />
   );
}