import { PlaceholderPage } from "../../../shared/components/PlaceholderPage";

export function MetricsPage() {
   return (
      <PlaceholderPage
         title="Metrics"
         description="Win rate, profit factor, drawdown, equity curve y rendimiento por estrategia."
      />
   );
}